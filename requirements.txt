# Install PyTorch separately before this file so torch, CUDA, and triton stay
# mutually compatible. See https://pytorch.org/get-started/locally/ for the
# right PyTorch command.

transformers==4.49.0
lm_eval==0.4.8
accelerate==0.34.2
hf_xet
pydantic==2.10.6
gradio
ninja
wheel
pytest
